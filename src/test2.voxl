class Vec2 {
	$init(x, y) {
		this.x = x;
		this.y = y;
		ret this;
	}

	$add(rhs) {
		ret Vec2(this.x + rhs.x, this.y + rhs.y);
	}

	log() {
		print("(" ++ this.x ++ ", " ++ this.y ++ ")");
	}
}

(Vec2(1, 2) + Vec2(2, 3) + Vec2(3, 4)).log();
